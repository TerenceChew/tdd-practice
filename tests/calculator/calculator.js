const calculator = {
  add(n1, n2) {
    return Number(n1) + Number(n2);
  },
  subtract(n1, n2) {
    return Number(n1) - Number(n2);
  },
  multiply(n1, n2) {
    return Number(n1) * Number(n2);
  },
  divide(n1, n2) {
    return Number(n1) / Number(n2);
  },
};

export default calculator;
